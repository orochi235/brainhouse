node_modules/
dist/
build/
# Ladle build cache is gitignored, but .ladle/components.tsx (and any
# other tracked config in there) needs to be checked in.
.ladle/cache/
.vite/
*.tsbuildinfo
coverage/
.DS_Store
.env
.env.local
.hued

# Playwright MCP traces from local smoke tests
.playwright-mcp/
*.png
# …except the README screenshots
!docs/screenshots/*.png
